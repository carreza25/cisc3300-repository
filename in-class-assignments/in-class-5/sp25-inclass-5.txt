Hex values represent values for red, green, and blue in hexadecimal code.
Typography can be defined as "the art and technique of arranging type in order to make language visible".
The font-face rule sets a new font to use that is made via font family. 
List-style shorthand property allows you to set all the list style properties at once.
The object-fit CSS property sets how the content of a replaced element, such as an <img> or ‹video>, should be resized to fit its container.